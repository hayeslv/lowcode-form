import type { IBaseNode } from "~/lowform-meta/type";
import { EComponentType } from "~/lowform-meta/type";

const data: IBaseNode = {
  group: EComponentType.FORM,
  key: "input",
  label: "输入框",
};

export default data;
